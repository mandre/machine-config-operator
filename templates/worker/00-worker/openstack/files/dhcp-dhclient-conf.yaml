filesystem: "root"
mode: 0644
path: "/etc/dhcp/dhclient.conf"
contents:
  inline: |
    send dhcp-client-identifier = hardware;
    prepend domain-name-servers 10.0.128.6;
    supersede domain-search "mandre.shiftstack.com";
