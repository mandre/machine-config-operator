filesystem: "root"
mode: 0644
path: "/etc/kubernetes/static-pod-resources/haproxy/config/haproxy.cfg"
contents:
  inline: |
    # NOTE(shadower): this is the INITIAL haproxy.cfg created by the MCO
    listen tsedovic.shiftstack.io-api-masters
        bind 0.0.0.0:7443
        mode tcp
        balance roundrobin
        server lb-api-vip-6443 {{ .Infra.Status.PlatformStatus.OpenStack.ApiIntIp }} check port 6443
